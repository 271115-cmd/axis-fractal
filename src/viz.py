"""
viz.py — all figures.  [Implemented in PHASE 7, used throughout]

WHAT THIS MODULE WILL DO
    One place for every figure so they share a consistent, portfolio-grade style:
    binary transect maps, log-log fit plots, families of Λ(r) curves by zone,
    per-tile boxplots, and a lacunarity heatmap over the study area. Clean, light
    background, minimal ink, consistent typography.

WHY CENTRALIZE PLOTTING
    Keeping styling in one module means every figure in the paper and portfolio
    looks like it belongs to the same study — and a style tweak changes all of them
    at once.

STATUS: stub. Small helpers may appear earlier; the full figure set lands in Phase 7.
"""

def main() -> None:
    raise NotImplementedError(
        "viz.py is a Phase 7 stub. We'll build figures incrementally as each phase produces data."
    )


if __name__ == "__main__":
    main()
